before_install:
 - sdk install java 9.0.1-open
 - sdk use java 9.0.1-open
install:
  - mvn -version
  - mvn clean install -DskipTests -Dcheckstyle.skip -Dspotbugs.skip -Dmaven.javadoc.skip -P release
